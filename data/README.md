Put the final Twitter/Sentiment140 CSV here as `twitter_sentiment.csv`.
Required columns: `tweet`, `sentiment`.
Optional column: `timestamp`.

The included `twitter_sentiment_sample.csv` is only for testing the project structure; it is not suitable for reporting final accuracy.
